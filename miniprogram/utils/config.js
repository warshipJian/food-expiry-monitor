// Developer Tools can access localhost. For a real device, replace this with
// your computer's LAN HTTPS/tunnel address and enable device debugging.
module.exports = { baseURL: 'http://localhost:8080' }
